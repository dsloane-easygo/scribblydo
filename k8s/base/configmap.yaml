apiVersion: v1
kind: ConfigMap
metadata:
  name: scribblydo-config
  namespace: scribblydo
  labels:
    app.kubernetes.io/name: scribblydo
    app.kubernetes.io/part-of: scribblydo-application
data:
  # Backend configuration (matches FastAPI app/config.py expectations)
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "tododb"

  # Frontend configuration
  BACKEND_URL: "http://backend-service:8000"

  # Application settings
  DEBUG: "false"
  CORS_ORIGINS: '["*"]'
  ENVIRONMENT: "production"

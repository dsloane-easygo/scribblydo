services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=scribblydo
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DEBUG=false
      - CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
      - SECRET_KEY=dev-secret-key-change-in-production
      - NATS_URL=nats://nats:4222
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_started
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - scribblydo-network

  nats:
    image: nats:2.10-alpine
    ports:
      - "4222:4222"
      - "8222:8222"
    command: ["--jetstream", "--http_port", "8222"]
    restart: unless-stopped
    networks:
      - scribblydo-network

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: scribblydo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-test-db.sh:/docker-entrypoint-initdb.d/init-test-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d scribblydo"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - scribblydo-network

  # Migration service - runs once and exits before backend starts
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: ["alembic", "upgrade", "head"]
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=scribblydo
      - DB_USER=postgres
      - DB_PASSWORD=postgres
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - scribblydo-network

  # Test runner service - runs e2e tests
  test:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: ["pytest", "-v", "--tb=short"]
    environment:
      - TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/scribblydo_test
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=scribblydo_test
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - SECRET_KEY=test-secret-key
      - NATS_URL=nats://nats:4222
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_started
    networks:
      - scribblydo-network
    profiles:
      - test

volumes:
  postgres_data:

networks:
  scribblydo-network:
    driver: bridge
